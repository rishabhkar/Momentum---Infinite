using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CollisionForBody : MonoBehaviour
{
   /* private PlayerMovement _crypto;
    private bool isGroundedCheck;

    // Start is called before the first frame update
    void Start()
    {
        _crypto = GameObject.Find("Crypto").GetComponent<PlayerMovement>();
    }

    // Update is called once per frame
    void Update()
    {
        isGroundedCheck = _crypto.isGrounded();
    }

    private void OnCollisionEnter(Collision collision)
    {
        if ((collision.gameObject.tag == "Ground") && _crypto.isGrounded() == true)
        {
            NotifyZAxisPush();            
        }
    }

    public bool NotifyZAxisPush()
    {
        return true;
    }
    public Vector3 currentBodyTransform()
    {
        return gameObject.transform.position;
    }*/
}
